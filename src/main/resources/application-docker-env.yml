spring:
  datasource:
    url: jdbc:postgresql://algashop-postgres:5432/billing
  flyway:
    enabled: true
    locations: classpath:db/migration,classpath:db/testdata
    url: jdbc:postgresql://algashop-postgres:5432/billing

algashop:
  integrations:
    payments:
      fastpay:
        hostname: http://wiremock:8080
        webhook-url: http://billing:8082/api/v1/payments/webhooks/fastpay